extends layout

block content
  div(class="mainContentDiv")
    p(class="pText") Search Results For: "#{search}"
    if questions.length
        each question in questions
            div(class="questionDiv")
                a(href=`/questions/${question.id}` class="questionText qContent") #{question.content}
                div
                    span(class="askedBy") User:
                    span(class="userTag") #{question.User.username}
                    span(class="userTag tag" id=`${question.Tag.tagName}`) ##{question.Tag.tagName}
    else
        div(class="noSearchText") The truth is out there. Just not here. Try searching for something else 👽👻
